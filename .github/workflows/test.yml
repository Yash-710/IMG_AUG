name: Website Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Check fake file exists
        run: test -f abcxyz.html

      - name: Check title exists
        run: grep -q "<title>" index.html